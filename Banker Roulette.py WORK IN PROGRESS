
names_string = input('Enter the player\'s names, put a comma after each player')
names = names_string.split(", ")

import random
random_integer = random.randint(0, len(names))
x = int(random_integer)

if random_integer == x:
    print(names[int(x)] + ' is going to buy the meal today!')
